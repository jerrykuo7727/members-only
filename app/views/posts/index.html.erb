<div>
  <%= link_to "Homepage", root_url %>
  <%= link_to "Sign in", signin_path %>
  <%= link_to "Sign out", signout_path, method: "delete" %>
  <p>Login status: <%= "true" if session[:user_id] %></p>
</div>

<div>
  <% @posts.each do |post| %>
    <p>------------------------------------------------------------------</p>
    <h2><%= post.title %></h2>
    <p><%= post.text %></p>
  <% end %>
  <p>------------------------------------------------------------------</p>
</div>